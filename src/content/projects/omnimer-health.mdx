---
title: Agentic AI Personal Health Management
projectId: PROJECT_OMNIMER
order: 1
featured: true
type: Personal project
typeVi: Dự án cá nhân
period: MAR — JUN 2026
role: Solo Developer & Full Stack AI Engineer
summary: A personal health platform combining real-time wearable processing, personalized anomaly detection and a safety-aware HybridRAG agent.
summaryVi: Nền tảng sức khỏe cá nhân kết hợp xử lý dữ liệu wearable thời gian thực, phát hiện bất thường cá nhân hóa và HybridRAG agent chú trọng an toàn.
technologies:
  - FastAPI
  - LangGraph
  - Node.js
  - Kafka
  - Redis
  - Qdrant
  - Neo4j
  - Flutter
metrics:
  - value: "0.85"
    label: Answer relevancy
    labelVi: Độ liên quan
  - value: "0.78"
    label: Faithfulness
    labelVi: Độ trung thực
  - value: "90%"
    label: Safety recall
    labelVi: Safety recall
links:
  source: https://github.com/HuynhTri-dev/omnimer_health
icon: /projects/omnimerhealth_mobile.png
image: /projects/omnimerhealth_web.jpg
seoKeywords:
  - Agentic AI
  - Health Management
  - HybridRAG
  - Wearable Processing
  - FastAPI
---

## Problem Statement & Scope

Existing digital health solutions currently face three significant market gaps:

- **Passive Traditional Health Apps:**  
  Platforms like MyFitnessPal primarily focus on logging and displaying static metrics (calories, heart rate, workouts). The data remains fragmented, lacks deep clinical analysis, and rarely translates into actionable daily habits. According to a user survey conducted via Google Forms, **61.5% of users stopped using or uninstalled their health apps** because they failed to provide enough continuous value to sustain long-term engagement.

- **Generic AI Models Lack Personalization and Safety:**  
  Generative AI tools like ChatGPT can answer questions quickly, but rely heavily on manual user prompts. Lacking longitudinal health profiles, historical activity baselines, and clinical safeguards, their advice is often generic or potentially unsafe for medical edge cases.

- **High Cost of Human Expert Consultation:**  
  Medical professionals and personal trainers offer deep, tailored guidance, but their services are expensive and cannot provide 24/7 continuous monitoring.

**OmniMer Health** bridges these gaps by delivering an **AI-driven Digital Health Coach**. It fuses real-time wearable biometric streaming with **Agentic AI** to proactively assist users with nutrition, workout planning, and daily lifestyle habits.

> **My Role:** As the sole **Full Stack AI & Mobile Engineer**, I owned the end-to-end product development—ranging from user need validation via Google Forms, microservices backend architecture, and AI reasoning pipelines, to the Flutter mobile client implementation.

## System Architecture Overview

<div class="my-8 rounded-2xl overflow-hidden border border-slate-800 bg-slate-950/50 p-2">
  <img src="/omnimer_health/overview_architecture.png" alt="OmniMer Health Overview Architecture" class="w-full h-auto rounded-xl object-contain" />
  <p class="text-center text-sm text-slate-400 mt-2 italic">Figure 1: High-level OmniMer Health system architecture built on three core pillars</p>
</div>

OmniMer Health is engineered around three strategic pillars:

1. **Vital Rules Engine (VRE) – Real-Time Sensing Layer:**  
   Ingests and processes continuous biometric streams (Heart Rate, SpO2, HRV) from wearables to compute adaptive personal baselines and detect anomalies in real time.

2. **Expert Think Agent – Analytical Reasoning Brain:**  
   Orchestrates complex multi-step reasoning using a 3-tier memory model (Short-Term, Long-Term, Context Window) combined with **Hybrid RAG** (Qdrant Vector DB + Neo4j Knowledge Graph).

3. **Personalized Safety Guard (PSG) – Safety & Compliance Layer:**  
   Enforces strict medical guardrails to ensure all recommendations support everyday wellness without replacing professional medical diagnosis.

## Vital Rules Engine (VRE) – Real-Time Sensing Layer

<div class="my-8 rounded-2xl overflow-hidden border border-slate-800 bg-slate-950/50 p-2">
  <img src="/omnimer_health/vre_architecture.png" alt="VRE Architecture" class="w-full h-auto rounded-xl object-contain" />
  <p class="text-center text-sm text-slate-400 mt-2 italic">Figure 2: Real-time biometric stream processing and adaptive baseline updating pipeline</p>
</div>

The VRE operates on a dual-path architecture to achieve immediate emergency alerting while maintaining long-term baseline precision:

### Fast Path (Node.js & BullMQ)
- **Ingestion:** A Node.js API accepts biometric events from mobile clients, enqueuing them into Redis BullMQ (`VitalQueueService.ts`) with exponential backoff retries, instantly returning `202 Accepted` to prevent I/O blocking.
- **Static Threshold Evaluation:** During onboarding before personalized baselines are calibrated, static clinical thresholds protect life safety (e.g., critical alerts when resting heart rate is below 40 bpm or above 150 bpm, or SpO2 drops below 90%).

### Deep Analytics Pipeline (Kafka Stream & Python Worker)
Kafka messages (`topic.vital.stream`) are consumed by a Python worker (`vital_rules_worker.py`) across a 5-stage pipeline:
1. **Noise Filtering:** Discards physically invalid readings (e.g., Heart Rate outside 30-220 bpm) and removes transient spikes (spike detection triggers if readings shift >50% within 60s).
2. **Adaptive Z-Score Evaluation:**  
   Computes standard deviation offset from the user's baseline:

   $$
   Z = \frac{x - \mu}{\max(\sigma, 0.01)}
   $$

   Risk categorization: **Critical** (`|Z| ≥ 3.0`), **Warning** (`2.0 ≤ |Z| < 3.0`), and **Normal** (`|Z| < 2.0`).
3. **Action Dispatching:** Manages notification deduplication via Redis time-bucket locks before dispatching APNs push notifications.
4. **Online Baseline Updates (Incremental Welford's Algorithm):**  
   Updates rolling baseline statistics directly on Redis in `O(1)` time without storing full raw event histories:

   $$
   \begin{aligned}
   \mu_{\mathrm{new}} &= \mu_{\mathrm{old}} + \frac{\Delta}{n_{\mathrm{new}}} \\
   M_{2,\mathrm{new}} &= M_{2,\mathrm{old}} + \Delta \cdot (x - \mu_{\mathrm{new}})
   \end{aligned}
   $$

5. **Short-Term Memory (STM) Synchronization:** Aggregates daily health flags into Redis buffers for zero-latency LLM context retrieval.

### Calibration & Adaptive Feedback Loop
- **Nightly Population Baseline Sync:** Celery Beat jobs compute 7-day historical population statistics (`N`) from MongoDB at 00:00 UTC, synchronizing baseline parameters back to Redis.
- **Adaptive Threshold Adjustment:** Incorporates user feedback (`False Alarm`, `Acknowledged`, `Escalated911`) to dynamically tune individual sensitivity multipliers (`sigmaMultiplier`), reducing false positive alerts over time.

## Expert Think Agent & Hybrid RAG – Reasoning Core

<div class="my-8 grid grid-cols-1 md:grid-cols-2 gap-4">
  <div class="rounded-2xl overflow-hidden border border-slate-800 bg-slate-950/50 p-2">
    <img src="/omnimer_health/memory_architect.png" alt="Memory Architecture" class="w-full h-auto rounded-xl object-contain" />
    <p class="text-center text-sm text-slate-400 mt-2 italic">Figure 3: 3-Tier Memory Architecture (STM, LTM & Context Window)</p>
  </div>
  <div class="rounded-2xl overflow-hidden border border-slate-800 bg-slate-950/50 p-2">
    <img src="/omnimer_health/agent_workflow.png" alt="Agent Workflow" class="w-full h-auto rounded-xl object-contain" />
    <p class="text-center text-sm text-slate-400 mt-2 italic">Figure 4: LangGraph Multi-turn ReAct & Reflexion Loop Execution</p>
  </div>
</div>

### StateGraph & Reflexion Loop (LangGraph)
The agent operates as a multi-step reasoning graph built with LangGraph:
- **Context Assembly:** Combines real-time STM states, LTM profiles (medical history and dietary preferences in Qdrant), and graph context from Neo4j.
- **Longitudinal Health Analysis:** Analyzes temporal trends. Critical anomalies immediately route execution to Human-in-the-Loop (HITL) checkpoints.
- **Multi-turn ReAct & Tool Execution:** Leverages domain tools (`calculate_bmr_tool`, `calculate_ncs_tool`, `query_recent_workout_summary`).
- **Reflexion Pipeline:** Evaluates draft outputs (`reflection_evaluation_node`). Safety violations or allergen conflicts trigger a **CRITICAL** flag, invoking self-refinement (`self_refine`) or emergency circuit breaking (`circuit_breaker`).

### Coaching Frameworks (FITT & Nutrition)
- **FITT Framework (Workout):** Pre-fills **F**requency, **I**ntensity, **T**ime, and **T**ype workout dimensions tailored to user activity levels (Sedentary, LightlyActive, VeryActive).
- **Nutrition Framework:** Computes clinical energy needs (BMR/NCS) and target macronutrient distribution ratios:
  - *Weight Loss (Cut):* 40% Protein - 30% Carb - 30% Fat.
  - *Muscle Gain (Bulk):* 30% Protein - 50% Carb - 20% Fat.
  - *Maintenance (Default):* 30% Protein - 40% Carb - 30% Fat.

### Hybrid RAG & Weighted Reciprocal Rank Fusion (WRRF)
Instead of relying on heavy, slow Cross-Encoder rerankers, OmniMer Health fuses **Qdrant Vector DB** (BGE-M3 Dense + BM25 Sparse) with **Neo4j Knowledge Graph** traversals via **Weighted Reciprocal Rank Fusion (WRRF)**:

$$
\mathrm{WRRF}_{\mathrm{Score}}(d)
=
\sum_{s \in \{\mathrm{dense},\,\mathrm{sparse},\,\mathrm{alias}\}}
\frac{w_s}{k + \operatorname{rank}_s(d)}
$$

- **Smoothing Constant:** `k = 60`.
- **Weights:** `w_dense = 1.0`, `w_sparse = 1.0`, and a boosted **`w_alias = 2.5`** to guarantee exact entity matches rank at the top.
- **Canonical Grouping & Query Facet Mapping:** Consolidates entity chunks into unified resource representations while filtering context by intent facets (`mistakes`, `safety`, `steps`, `nutrients`).

## UX/UI Design & System Philosophy

To ensure OmniMer Health is not only technically robust but also highly accessible and engaging, I established a comprehensive UX/UI design system. The goal is to turn the app into a true "Companion" rather than a dry clinical measurement tool.

<div class="my-8 flex w-full snap-x snap-mandatory gap-4 overflow-x-auto pb-4">
  <div class="w-[85%] md:w-[calc(33.333%-11px)] flex-shrink-0 snap-start overflow-hidden rounded-2xl border border-slate-800 bg-slate-950/50">
    <img src="/omnimer_health/design_1.jpg" alt="Design System 1" class="h-auto w-full object-cover" loading="lazy" />
  </div>
  <div class="w-[85%] md:w-[calc(33.333%-11px)] flex-shrink-0 snap-start overflow-hidden rounded-2xl border border-slate-800 bg-slate-950/50">
    <img src="/omnimer_health/design_2.jpg" alt="Design System 2" class="h-auto w-full object-cover" loading="lazy" />
  </div>
  <div class="w-[85%] md:w-[calc(33.333%-11px)] flex-shrink-0 snap-start overflow-hidden rounded-2xl border border-slate-800 bg-slate-950/50">
    <img src="/omnimer_health/design_3.jpg" alt="Design System 3" class="h-auto w-full object-cover" loading="lazy" />
  </div>
  <div class="w-[85%] md:w-[calc(33.333%-11px)] flex-shrink-0 snap-start overflow-hidden rounded-2xl border border-slate-800 bg-slate-950/50">
    <img src="/omnimer_health/design_4.jpg" alt="Design System 4" class="h-auto w-full object-cover" loading="lazy" />
  </div>
  <div class="w-[85%] md:w-[calc(33.333%-11px)] flex-shrink-0 snap-start overflow-hidden rounded-2xl border border-slate-800 bg-slate-950/50">
    <img src="/omnimer_health/design_5.jpg" alt="Design System 5" class="h-auto w-full object-cover" loading="lazy" />
  </div>
  <div class="w-[85%] md:w-[calc(33.333%-11px)] flex-shrink-0 snap-start overflow-hidden rounded-2xl border border-slate-800 bg-slate-950/50">
    <img src="/omnimer_health/design_6.jpg" alt="Design System 6" class="h-auto w-full object-cover" loading="lazy" />
  </div>
</div>

### Design Style: Bento Grid & Minimalist Art

The interface employs a **Bento Grid combined with Minimalist Art**. By dividing the layout into neatly arranged, rounded cards—reminiscent of a Japanese Bento box—the application creates a comfortable, structured, and easy-to-digest experience for the user. 
Implemented in Flutter using `GridView` and `StaggeredGridView`, this approach maintains distinctly separated information cards with large, friendly rounded corners.

### Color Palette, Typography & Iconography

*   **Primary Colors**: **Medical Blue (`#1F4FD8`)** and **Light Blue (`#E9F0FF`)** establish trust and are used for primary actions, navigation, and health chart backgrounds.
*   **AI Accent Colors**: **AI Purple (`#6C63FF`)** and **AI Soft (`#F1F0FF`)** are dedicated strictly to the AI Agent—used for suggestions, chat bubbles, and analysis effects.
*   **Typography**: **Be Vietnam Pro** is used for branding and main content due to its excellent handling of Vietnamese diacritics and modern feel. Meanwhile, **Inter** is applied to data and metrics (charts, blood pressure) for clear numerals and quick scanning.
*   **Iconography**: We utilize **Material Symbols (Rounded & Filled)**. The rounded corners evoke friendliness, while the "Filled" style ensures high legibility even for users with poor vision or in bright outdoor environments.

### Core Design Principles

1.  **"The Thumb Zone" Rule**: Critical interactions—like the Scan Button, AI Chat Button, and main navigation tabs—are securely placed in the bottom 1/3 of the screen. Navigation relies heavily on intuitive swipe gestures, minimizing the need to reach for top-corner "Back" buttons.
2.  **Smart Input & Masking**: Health input fields instantly trigger numeric keypads and automatically format data (e.g., typing "12080" instantly formats to "120/80 mmHg"). For prescription scanning, an OCR Overlay guides the user: *"Keep your hand steady, AI is reading the data..."*
3.  **8-Point Grid System**: All spatial properties (padding and margins) strictly adhere to multiples of 8px. This not only guarantees perfect visual balance but also keeps the Flutter UI codebase exceptionally clean.

### Data Visualization & The Human Touch

*   **Actionable AI Interpreter**: Instead of simply displaying raw metrics, the AI acts as a sidekick. For instance, rather than a generic "High GI Index" warning, a friendly AI avatar advises: *"This dish is a bit high in sugar; you should eat some green vegetables with it to balance it out!"*
*   **Visualizing Progress**: We use soft-curved line charts for tracking trends and donut charts for daily goals. Completing a goal triggers micro-interaction fireworks to reward the user.
*   **Gamification & Skeletons**: To drive engagement, the app features a "Streaks" system. During complex AI processing, a pulsing skeleton loading frame is displayed, making the system feel active and "thinking" rather than stalling.

## The Interactive AI Agent Experience

To clearly distinguish the AI's role from the standard app UI, the Agent is designed to act as a living, breathing companion.

*   **Dynamic Persona**: Instead of a simple chat interface, the agent is represented through minimalist line art with smooth motion effects. For example, the character might appear to be exercising when you ask for a workout plan.
*   **Contextual Presence**: The agent remains hidden when not in use. Upon interaction, it elegantly overlays on any page with smooth transitions. It even supports thematic outfit changes based on the user's current health goals.
*   **Advanced Capabilities**: The interface includes seamless triggers to switch between different agent support modes. It is also designed to accommodate future features like document/image uploads and incorporates specific **Human-in-the-Loop (HITL)** widgets for scenarios requiring human expert validation.

## Evaluation & Benchmarks

<div class="my-8 grid grid-cols-1 md:grid-cols-2 gap-4">
  <div class="rounded-2xl overflow-hidden border border-slate-800 bg-slate-950/50 p-2">
    <img src="/omnimer_health/ragas_benchmark.png" alt="RAGAS Benchmark" class="w-full h-auto rounded-xl object-contain" />
    <p class="text-center text-sm text-slate-400 mt-2 italic">Figure 5: RAG Quality Evaluation across RAGAS Metrics</p>
  </div>
  <div class="rounded-2xl overflow-hidden border border-slate-800 bg-slate-950/50 p-2">
    <img src="/omnimer_health/safety_eval.png" alt="Safety Evaluation" class="w-full h-auto rounded-xl object-contain" />
    <p class="text-center text-sm text-slate-400 mt-2 italic">Figure 6: Safety Recall and Useful Response Rate comparison</p>
  </div>
</div>

### RAG Retrieval & Reasoning Quality (RAGAS Framework)
Evaluated against a curated benchmark of 100 medical health questions with verified ground truths:

- **Context Precision (0.69):** Significantly improved from the **0.42** baseline, proving that Hybrid RAG retrieves exact context while eliminating irrelevant noise.
- **Context Recall (0.66):** Ensures comprehensive retrieval of medical guidelines and user background.
- **Faithfulness (0.78):** Demonstrates strict alignment with retrieved evidence, eliminating hallucinations.
- **Answer Relevancy (0.85):** Highlights sharp focus on user queries driven by tool execution and self-reflection loops.

### Safety Evaluation & Medical Guardrails
Tested against 20 high-risk clinical scenarios:

- **Baseline LLM:** Achieved only **~5% safety recall**, frequently generating dangerous advice during acute medical symptoms.
- **Optimized CoT:** Reached **100% safety recall** but proved over-conservative, rejecting benign lifestyle questions and hurting user experience.
- **OmniMer Health:** Achieved **~90% Safety Recall**, striking an optimal balance between **medical safety** and **interactive user utility**.

## Future Roadmap

1. **Multi-Device & IoT Integration:** Expand ingestion capabilities to Garmin, Fitbit, Samsung Health, and specialized clinical IoT devices (such as Continuous Glucose Monitors - CGM).
2. **Clinical Expert Evaluation Dataset:** Collaborate directly with physicians, dietitians, and sports specialists to build an expert-annotated evaluation benchmark.
3. **Post-Retrieval Reranking Optimization:** Integrate `BGE-Reranker-v2-M3` post-retrieval to optimize document order before passing context to LLMs.
4. **Semi-Automated Knowledge Graph Ingestion:** Automate graph extraction from PubMed and NIH guidelines using a **Human-in-the-Loop (HITL)** expert validation workflow.
